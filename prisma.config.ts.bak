export default defineConfig({
  schema: "prisma/schema.prisma",
  migrations: {
    path: "prisma/migrations",
  },
  engine: "classic",
  datasource: {
    // CHANGE THIS: Use the name of the variable in your .env file
    url: env("DATABASE_URL"), 
  },
});